# Build requirements for creating Windows executable
# These are additional dependencies needed for the build process

# PyInstaller - Python to executable converter
pyinstaller>=5.13.0

# Core runtime dependencies (from main requirements.txt)
PySide6>=6.5.0
SQLAlchemy>=1.4.0
PyMuPDF>=1.21.0
openpyxl>=3.0.10
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
python-dateutil>=2.8.0

# Additional build-time dependencies
# Pillow - Image processing (often needed by PyInstaller for icons)
Pillow>=9.0.0

# Additional hidden imports that PyInstaller might miss
# These will be specified in the spec file but good to have available
setuptools>=65.0.0